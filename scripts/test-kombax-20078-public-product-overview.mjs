import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');
const config=read('web/config.js'),index=read('web/index.html'),worker=read('web/service-worker.js'),gradle=read('android/app/build.gradle'),overview=read('web/js/public-product-overview.js'),css=read('web/css/kombax-public-overview.css');
const build=Number(config.match(/build:\s*(\d+)/)?.[1]),android=Number(gradle.match(/versionCode\s+(\d+)/)?.[1]);assert.ok(build>=20078);assert.equal(android,build);assert.ok(index.includes(`kombax-public-overview.css?v=${build}`));assert.ok(index.includes(`public-product-overview.js?v=${build}`));assert.ok(worker.includes(String(build)));for(const x of ['CLUBES','FEDERACIONES','COMPETIDORES','MARCAS','gateway-club','gateway-direct','featureIcon','deportes de contacto y artes marciales'])assert.ok(overview.includes(x),x);assert.ok(css.includes('.kx-product-card.is-featured'));assert.ok(css.includes('@media(max-width:620px)'));assert.ok(css.includes('prefers-reduced-motion'));console.log(`KOMBAX ${build} public product overview: PASS`);
