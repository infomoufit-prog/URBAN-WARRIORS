# SQL PATCH 043 / 046 · 2026-08-17

Durante la ejecución real de `043_kombax_profiles_verification_album.sql` en Supabase, PostgreSQL devolvió:

`ERROR 42601: syntax error at or near "position"`

Causa: `position` se usaba sin comillas como nombre de columna de salida dentro de una firma `RETURNS TABLE`.

Correcciones aplicadas:
- `043_kombax_profiles_verification_album.sql`: `position integer` → `"position" integer` en `app_kombax_album_v043`.
- `046_kombax_club_album.sql`: misma corrección preventiva en `app_kombax_club_album_v046`.

No se ha alterado ninguna otra lógica de las migraciones.
