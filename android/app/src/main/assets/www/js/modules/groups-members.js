import { repos } from '../core/repositories.js';
import { state } from '../core/state.js';
import { has } from '../core/permissions.js';
import { esc, byName, dateFmt, isoDate } from '../core/utils.js';
import { pageHeader, progress, quickRow, card, table, empty, badge, openForm, openDetail, confirmDialog, toast, setError, setMainHtml } from '../ui/components.js';
import { icon } from '../ui/icons.js';

const DAYS=['','Lunes','Martes','Miércoles','Jueves','Viernes','Sábado','Domingo'];
const DOC_TYPES=[['inscripcion_asociacion','Inscripción asociación'],['contrato_alta','Ficha / contrato de alta'],['autorizacion','Autorización'],['consentimiento','Consentimiento'],['tutor','Documentación tutor/a'],['certificado','Certificado'],['medico','Documento médico'],['identidad','Identidad'],['otro','Otro']];
const bind=(selector,fn)=>document.querySelectorAll(selector).forEach(el=>el.addEventListener('click',()=>fn(el.dataset.id,el)));
const options=(rows,label=(r)=>r.nombre)=>rows.map(r=>({value:r.id,label:label(r)}));
const isDirection=()=>((state.session?.roles?.length?state.session.roles:[state.session?.rol]).filter(Boolean)).includes('direccion');
const forceConfirm=(title,subtitle,onConfirm)=>openForm({title,subtitle:`${subtitle} Escribe ELIMINAR para confirmar.`,fields:[{name:'confirmacion',label:'Confirmación',required:true,placeholder:'ELIMINAR',help:'Esta acción no se puede deshacer.'}],submitText:'Eliminar todo definitivamente',onSubmit:async v=>{if(String(v.confirmacion||'').trim().toUpperCase()!=='ELIMINAR')throw new Error('Escribe ELIMINAR exactamente para confirmar.');return onConfirm();}});

function groupFields(disciplines,schedules=[]){
  const rows=[0,1,2,3,4,5,6].map(i=>schedules[i]||{});
  const f=[
    {name:'disciplina_id',label:'Disciplina',type:'select',required:true,options:options(disciplines.filter(d=>d.activa))},
    {name:'nombre',label:'Nombre',required:true},{name:'monitor_nombre',label:'Monitor/a'},{name:'sala',label:'Sala'},
    {name:'edad_min',label:'Edad mínima',type:'number',min:0},{name:'edad_max',label:'Edad máxima',type:'number',min:0},{name:'plazas',label:'Plazas',type:'number',min:1},{name:'activo',label:'Grupo activo',type:'checkbox',value:true}
  ];
  rows.forEach((r,i)=>{const n=i+1;f.push({name:`dia_${n}`,label:`Horario ${n} · día`,type:'select',options:DAYS.slice(1).map((d,j)=>({value:j+1,label:d})),value:r.dia_semana||''});f.push({name:`inicio_${n}`,label:`Horario ${n} · inicio`,type:'time',value:r.hora_inicio?.slice(0,5)||''});f.push({name:`fin_${n}`,label:`Horario ${n} · fin`,type:'time',value:r.hora_fin?.slice(0,5)||''});});
  return f;
}
function collectSchedules(v){const out=[];for(let i=1;i<=7;i++){if(v[`dia_${i}`]&&v[`inicio_${i}`]&&v[`fin_${i}`])out.push({dia_semana:Number(v[`dia_${i}`]),hora_inicio:v[`inicio_${i}`],hora_fin:v[`fin_${i}`]});}return out;}

export async function renderGroups(){
  setMainHtml('<div class="loading-card">Cargando grupos…</div>');
  try{
    const [allGroups,schedules,disciplines,enrollments]=await Promise.all([repos.groups.list(),repos.groups.schedules(),repos.catalog.disciplines(),repos.members.enrollments()]);
    const groups=allGroups;groups.sort(byName);const can=has(state.session,'group');
    const cards=groups.map(g=>{const d=disciplines.find(x=>x.id===g.disciplina_id);const hs=schedules.filter(h=>h.grupo_id===g.id).map(h=>`${DAYS[h.dia_semana]} ${String(h.hora_inicio).slice(0,5)}–${String(h.hora_fin).slice(0,5)}`);const count=new Set(enrollments.filter(e=>e.grupo_id===g.id&&e.activa).map(e=>e.socio_id)).size;const cap=Number(g.plazas||0),pct=cap?Math.min(100,Math.round(count/cap*100)):0;return `<section class="card brand-card"><div class="card-head"><div><div class="page-kicker">${esc(d?.nombre||'Disciplina')}</div><h2 style="font-size:20px">${esc(g.nombre)}</h2></div><div class="row-actions">${badge(g.activo?'Activo':'Archivado',g.activo?'ok':'neutral')}${can?`<button class="btn btn-ghost btn-sm edit-group" data-id="${esc(g.id)}">${icon('edit',{size:14})} Editar</button><button class="btn btn-ghost btn-sm archive-group" data-id="${esc(g.id)}">${g.activo?'Archivar':'Reactivar'}</button><button class="btn btn-danger btn-sm delete-group" data-id="${esc(g.id)}">${icon('trash',{size:14})} Eliminar</button>${isDirection()?`<button class="btn btn-danger btn-sm force-delete-group" data-id="${esc(g.id)}">Eliminar todo</button>`:''}`:''}</div></div><div class="grid-2" style="gap:12px"><div><small class="muted">HORARIOS</small><p style="line-height:1.7;margin:8px 0 0">${hs.length?hs.map(esc).join('<br>'):'—'}</p></div><div><small class="muted">MONITOR / SALA</small><p style="line-height:1.7;margin:8px 0 0">${esc(g.monitor_nombre||'Sin asignar')}<br>${esc(g.sala||'—')}</p></div></div><div style="margin-top:18px;display:flex;justify-content:space-between;align-items:end"><div><small class="muted">OCUPACIÓN</small><strong style="display:block;font-size:24px;margin-top:4px">${count}/${g.plazas||'—'}</strong></div><span class="muted">${pct}%</span></div>${progress(pct)}</section>`}).join('');
    setMainHtml(`${pageHeader(state.session?.rol==='monitor'?'Mis grupos':'Grupos y horarios',state.session?.rol==='monitor'?'Grupos asignados a tu perfil':'Clases regulares, ocupación y responsables',can?'<button class="btn btn-primary" id="new-group">Nuevo grupo</button>':'','Club')}<div class="grid-2">${cards||empty('Sin grupos')}</div>`);
    const reload=()=>renderGroups();
    const open=(g={})=>openForm({title:g.id?'Editar grupo':'Nuevo grupo',fields:groupFields(disciplines,schedules.filter(h=>h.grupo_id===g.id)),initial:g,onSubmit:async v=>{await repos.groups.save({...g,...v,id:g.id||null,horarios:collectSchedules(v)});toast('Grupo guardado');await reload();},width:'840px'});
    document.getElementById('new-group')?.addEventListener('click',()=>open());bind('.edit-group',id=>open(groups.find(x=>x.id===id)));
    bind('.archive-group',id=>{const g=groups.find(x=>x.id===id);confirmDialog(g.activo?'Archivar grupo':'Reactivar grupo',g.activo?'El grupo dejará de ofrecerse para nuevas matrículas, pero conservará su histórico.':'El grupo volverá a estar activo.',async()=>{await repos.groups.save({...g,activo:!g.activo,horarios:schedules.filter(h=>h.grupo_id===g.id).map(h=>({dia_semana:h.dia_semana,hora_inicio:String(h.hora_inicio).slice(0,5),hora_fin:String(h.hora_fin).slice(0,5)}))});toast(g.activo?'Grupo archivado':'Grupo reactivado');await reload();},{confirmText:g.activo?'Archivar':'Reactivar'});});
    bind('.delete-group',id=>confirmDialog('Eliminar grupo','Se eliminará si no tiene histórico relacionado. Si tiene sesiones o matrículas, usa “Eliminar todo” únicamente si quieres borrar también ese histórico.',async()=>{await repos.groups.delete(id);toast('Grupo eliminado');await reload();},{confirmText:'Eliminar',danger:true}));
    bind('.force-delete-group',id=>forceConfirm('Eliminar grupo y todo su histórico','Se borrarán sesiones, asistencias/check-ins y matrículas vinculadas a este grupo. Las solicitudes conservarán al alumno pero perderán la referencia al grupo.',async()=>{await repos.groups.forceDelete(id);toast('Grupo e histórico eliminados');await reload();}));
  }catch(e){setError(e);setMainHtml(`${pageHeader('Grupos y horarios')} ${empty('No se pudieron cargar los grupos',e.message)}`)}
}

async function loadMemberRelations(){const [disciplines,grades,groups,tariffs]=await Promise.all([repos.catalog.disciplines(),repos.catalog.grades(),repos.groups.list(),repos.tariffs.list()]);return {disciplines,grades,groups,tariffs};}
function memberFields(r){return [
  {name:'nombre',label:'Nombre',required:true},{name:'apellidos',label:'Apellidos',required:true},{name:'fecha_nacimiento',label:'Fecha de nacimiento',type:'date'},
  {name:'telefono',label:'Teléfono'},{name:'email',label:'Email',type:'email'},{name:'tutor_nombre',label:'Tutor/a'},
  {name:'disciplina_id',label:'Disciplina',type:'select',options:options(r.disciplines.filter(d=>d.activa))},{name:'grupo_id',label:'Grupo',type:'select',options:options(r.groups.filter(g=>g.activo))},
  {name:'grado_id',label:'Grado',type:'select',options:options(r.grades.filter(g=>g.activo),g=>`${r.disciplines.find(d=>d.id===g.disciplina_id)?.nombre||''} · ${g.nombre}`)},
  {name:'tarifa_id',label:'Tarifa',type:'select',options:options(r.tariffs.filter(t=>t.activa),t=>`${t.nombre} · ${Number(t.importe||0).toFixed(2)} €`)},
  {name:'estado',label:'Estado',type:'select',required:true,options:['prealta','activo','baja','suspendido'].map(x=>({value:x,label:x}))},
  {name:'contacto_emergencia',label:'Contacto de emergencia'},{name:'telefono_emergencia',label:'Teléfono emergencia'},{name:'notas_internas',label:'Notas internas',type:'textarea',full:true}
]}

async function renderMonitorMembers(){
  setMainHtml('<div class="loading-card">Cargando tus alumnos…</div>');
  try{
    const members=await repos.scopes.students();
    const rows=(members||[]).map(m=>`<tr data-member-row data-search="${esc(`${m.nombre||''} ${m.apellidos||''} ${m.email||''} ${m.telefono||''} ${m.disciplinas||''} ${m.grupos||''}`.toLowerCase())}" data-status="${esc(m.estado||'')}"><td><strong>${esc(m.apellidos||'')}, ${esc(m.nombre||'')}</strong></td><td>${m.edad==null?'—':`${Number(m.edad)} años`}</td><td>${esc(m.disciplinas||'—')}<br><small>${esc(m.grupos||'')}</small></td><td>${m.email||m.telefono?`${esc(m.email||'')}${m.email&&m.telefono?'<br>':''}<small>${esc(m.telefono||'')}</small>`:'<span class="muted">Privado</span>'}</td><td>${badge(m.estado||'—',m.estado==='activo'?'ok':m.estado==='suspendido'?'warn':'neutral')}</td></tr>`);
    setMainHtml(`${pageHeader('Mis alumnos','Solo alumnos asignados a tus ámbitos o grupos. Los datos administrativos, familiares, documentales y financieros permanecen separados.','','Privacidad')}
      <div class="alert alert-info"><strong>Acceso por ámbito</strong><span>Si un alumno pertenece a otro monitor no aparecerá aquí. El contacto solo se muestra cuando el Gestor lo autoriza expresamente.</span></div>
      <div class="filter-bar"><input id="member-search" type="search" placeholder="Buscar por alumno, disciplina o grupo…"><select id="member-status"><option value="">Todos los estados</option>${['activo','prealta','suspendido','baja'].map(x=>`<option value="${x}">${x}</option>`).join('')}</select><span class="badge badge-neutral" id="member-count">${members.length} alumnos</span></div>
      ${card('Alumnos asignados',rows.length?table(['Alumno','Edad','Actividad','Contacto','Estado'],rows):empty('Sin alumnos asignados','Pide al Gestor o Coordinación que te asigne un ámbito, alumnos o grupos.'))}`);
    const applyFilter=()=>{const q=String(document.getElementById('member-search')?.value||'').trim().toLowerCase(),st=document.getElementById('member-status')?.value||'';let visible=0;document.querySelectorAll('[data-member-row]').forEach(row=>{const show=(!q||row.dataset.search.includes(q))&&(!st||row.dataset.status===st);row.style.display=show?'':'none';if(show)visible++;});const c=document.getElementById('member-count');if(c)c.textContent=`${visible} alumnos`;};
    document.getElementById('member-search')?.addEventListener('input',applyFilter);document.getElementById('member-status')?.addEventListener('change',applyFilter);
  }catch(e){setError(e);setMainHtml(`${pageHeader('Mis alumnos')} ${empty('No se pudieron cargar tus alumnos',e.message)}`)}
}

export async function renderMembers(){
  if(state.session?.rol==='monitor')return renderMonitorMembers();
  setMainHtml('<div class="loading-card">Cargando alumnos…</div>');
  try{
    const canCodes=['direccion','coordinacion'].includes(state.session?.rol);const [members,enrollments,relations,documents,accessCodes]=await Promise.all([repos.members.list(),repos.members.enrollments(),loadMemberRelations(),repos.documents.list().catch(()=>[]),canCodes?repos.accessCodes.get():Promise.resolve(null)]);const can=has(state.session,'member');
    const rowFor=(m)=>{const links=enrollments.filter(x=>x.socio_id===m.id&&x.activa);const names=links.map(l=>relations.disciplines.find(d=>d.id===l.disciplina_id)?.nombre).filter(Boolean).join(', ');const groups=links.map(l=>relations.groups.find(g=>g.id===l.grupo_id)?.nombre).filter(Boolean).join(', ');const docs=documents.filter(d=>d.socio_id===m.id&&d.estado!=='archivado'&&d.estado!=='sustituido');return `<tr data-member-row data-search="${esc(`${m.nombre} ${m.apellidos} ${m.email||''} ${m.telefono||''} ${names} ${groups}`.toLowerCase())}" data-status="${esc(m.estado)}"><td><strong>${esc(m.apellidos)}, ${esc(m.nombre)}</strong><br><small>${esc(m.email||m.telefono||'')}</small></td><td>${esc(names||'—')}<br><small>${esc(groups||'')}</small></td><td>${badge(m.estado,m.estado==='activo'?'ok':m.estado==='suspendido'?'warn':m.estado==='baja'?'neutral':'warn')}</td><td>${dateFmt(m.fecha_alta)}</td><td>${badge(`${docs.length} docs`,docs.length?'ok':'warn')}</td><td>${can?`<div class="row-actions"><button class="btn btn-ghost btn-sm edit-member" data-id="${esc(m.id)}">Editar</button><button class="btn btn-ghost btn-sm enroll-member" data-id="${esc(m.id)}">Matrícula</button>${links.length?`<button class="btn btn-ghost btn-sm deactivate-enrollment" data-id="${esc(m.id)}">Baja matrícula</button>`:''}<button class="btn btn-ghost btn-sm graduate-member" data-id="${esc(m.id)}">Graduación</button><button class="btn btn-ghost btn-sm document-member" data-id="${esc(m.id)}">Documento</button>${m.estado!=='baja'?`<button class="btn btn-ghost btn-sm archive-member" data-id="${esc(m.id)}">Dar de baja</button>`:''}<button class="btn btn-danger btn-sm delete-member" data-id="${esc(m.id)}">Eliminar</button>${isDirection()?`<button class="btn btn-danger btn-sm force-delete-member" data-id="${esc(m.id)}">Eliminar todo</button>`:''}</div>`:''}</td></tr>`};
    const headerActions=(can||canCodes)?`<div class="row-actions">${canCodes?'<button class="btn btn-ghost" id="student-access-code">Código alumnos/familias</button>':''}${can?'<button class="btn btn-primary" id="new-member">Nuevo alumno</button>':''}</div>`:'';setMainHtml(`${pageHeader(state.session?.rol==='monitor'?'Mis alumnos':'Alumnos','Fichas, matrículas, grados y expediente documental',headerActions,'Gestión')}<div class="filter-bar"><input id="member-search" type="search" placeholder="Buscar por nombre, email, disciplina o grupo…"><select id="member-status"><option value="">Todos los estados</option>${['activo','prealta','suspendido','baja'].map(x=>`<option value="${x}">${x}</option>`).join('')}</select><span class="badge badge-neutral" id="member-count">${members.length} alumnos</span></div>${card('Alumnos',members.length?table(['Alumno','Actividad','Estado','Alta','Expediente','Acciones'],members.map(rowFor)):empty('Sin alumnos'))}`);
    const applyFilter=()=>{const q=String(document.getElementById('member-search')?.value||'').trim().toLowerCase(),st=document.getElementById('member-status')?.value||'';let visible=0;document.querySelectorAll('[data-member-row]').forEach(row=>{const show=(!q||row.dataset.search.includes(q))&&(!st||row.dataset.status===st);row.style.display=show?'':'none';if(show)visible++;});const c=document.getElementById('member-count');if(c)c.textContent=`${visible} alumnos`;};document.getElementById('member-search')?.addEventListener('input',applyFilter);document.getElementById('member-status')?.addEventListener('change',applyFilter);
    const reload=()=>renderMembers();const openMember=(m={estado:'activo'})=>openForm({title:m.id?'Editar alumno':'Nuevo alumno',fields:memberFields(relations),initial:m,onSubmit:async v=>{await repos.members.save({...m,...v,id:m.id||null});toast('Alumno guardado');await reload();},width:'900px'});
    document.getElementById('new-member')?.addEventListener('click',()=>openMember());
    document.getElementById('student-access-code')?.addEventListener('click',()=>{const code=accessCodes?.alumnos?.codigo||'—';const detail=openDetail({title:'Código para alumnos y familias',subtitle:'Código reutilizable del club. Puedes imprimirlo junto al QR. Cambiarlo invalida inmediatamente el anterior.',body:`<div class="detail-grid"><div><small>Código actual</small><h3><code>${esc(code)}</code></h3></div><div><small>Uso</small><p>Alumnos de 16+ y padres/madres/tutores. Los menores siguen el flujo de tutor.</p></div></div><div class="row-actions"><button class="btn btn-primary" id="copy-student-access-code">Copiar código</button><button class="btn btn-ghost" id="rotate-student-access-code">Cambiar código</button></div>`});detail.wrap.querySelector('#copy-student-access-code')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(code);toast('Código copiado')}catch{prompt('Copia el código',code)}});detail.wrap.querySelector('#rotate-student-access-code')?.addEventListener('click',()=>openForm({title:'Cambiar código de alumnos/familias',subtitle:'Escribe 4 o 5 dígitos o déjalo vacío para generar automáticamente uno nuevo de 5 dígitos.',fields:[{name:'codigo',label:'Nuevo código',placeholder:'Ej. 31415',help:'Solo números · 4 o 5 dígitos'}],submitText:'Cambiar código',onSubmit:async v=>{const value=String(v.codigo||'').trim();if(value&&!/^\d{4,5}$/.test(value))throw new Error('El código debe tener 4 o 5 dígitos numéricos.');await repos.accessCodes.rotate('alumnos',value);toast('Código de alumnos/familias actualizado');await renderMembers();}}));});
    bind('.edit-member',id=>openMember(members.find(x=>x.id===id)));
    bind('.enroll-member',id=>openForm({title:'Añadir matrícula',subtitle:members.find(x=>x.id===id)?.nombre||'',fields:[{name:'disciplina_id',label:'Disciplina',type:'select',required:true,options:options(relations.disciplines.filter(d=>d.activa))},{name:'grupo_id',label:'Grupo',type:'select',required:true,options:options(relations.groups.filter(g=>g.activo))},{name:'tarifa_id',label:'Tarifa',type:'select',options:options(relations.tariffs.filter(t=>t.activa))}],onSubmit:async v=>{await repos.members.requestEnrollment(id,v.disciplina_id,v.grupo_id,v.tarifa_id);toast('Solicitud de matrícula registrada');await reload();}}));
    bind('.deactivate-enrollment',id=>{const links=enrollments.filter(x=>x.socio_id===id&&x.activa);openForm({title:'Desactivar matrícula',fields:[{name:'matricula_id',label:'Matrícula',type:'select',required:true,options:links.map(l=>({value:l.id,label:`${relations.disciplines.find(d=>d.id===l.disciplina_id)?.nombre||'Disciplina'} · ${relations.groups.find(g=>g.id===l.grupo_id)?.nombre||'Grupo'}`}))}],submitText:'Desactivar',onSubmit:async v=>{await repos.members.deactivateEnrollment(v.matricula_id);toast('Matrícula desactivada');await reload();}})});
    bind('.graduate-member',id=>openForm({title:'Registrar graduación',subtitle:members.find(x=>x.id===id)?.nombre||'',fields:[{name:'disciplina_id',label:'Disciplina',type:'select',required:true,options:options(relations.disciplines.filter(d=>d.activa))},{name:'grado_id',label:'Nuevo grado',type:'select',required:true,options:options(relations.grades.filter(g=>g.activo),g=>`${relations.disciplines.find(d=>d.id===g.disciplina_id)?.nombre||''} · ${g.nombre}`)},{name:'fecha',label:'Fecha',type:'date',required:true,value:isoDate()},{name:'examinador',label:'Examinador/a'},{name:'nota',label:'Nota',type:'textarea',full:true}],onSubmit:async v=>{await repos.members.graduation({...v,socio_id:id});toast('Graduación registrada');await reload();}}));
    bind('.document-member',id=>openForm({title:'Subir al expediente',subtitle:'Documentación privada del alumno',fields:[{name:'tipo',label:'Tipo',type:'select',required:true,options:DOC_TYPES.map(([value,label])=>({value,label}))},{name:'nombre',label:'Nombre del documento'},{name:'fecha_documento',label:'Fecha del documento',type:'date'},{name:'firmado',label:'Documento firmado',type:'checkbox',value:true},{name:'archivo',label:'Archivo',type:'file',required:true,accept:'.pdf,.jpg,.jpeg,.png,.webp',help:'PDF o imagen · máximo 10 MB'},{name:'visible_familia',label:'Visible para familia',type:'checkbox',value:true},{name:'observaciones',label:'Observaciones',type:'textarea',full:true}],onSubmit:async v=>{await repos.documents.upload(id,v.archivo,{...v,nombre:v.nombre||v.archivo.name});toast('Documento guardado en el expediente');await reload();}}));
    bind('.archive-member',id=>openForm({title:'Dar de baja al alumno',subtitle:'Se conserva todo su histórico de asistencia, pagos, recibos y documentación.',fields:[{name:'fecha_baja',label:'Fecha de baja',type:'date',value:isoDate(),required:true},{name:'motivo',label:'Motivo',type:'textarea',full:true,required:true}],submitText:'Dar de baja',onSubmit:async v=>{await repos.members.archive(id,v.motivo,v.fecha_baja);toast('Alumno dado de baja');await reload();}}));
    bind('.delete-member',id=>confirmDialog('Eliminar alumno','Se eliminará si no tiene histórico. Si tiene actividad, puedes darlo de baja o, solo desde Gestor de la app, usar “Eliminar todo”.',async()=>{await repos.members.delete(id);toast('Alumno eliminado');await reload();},{confirmText:'Eliminar',danger:true}));
    bind('.force-delete-member',id=>forceConfirm('Eliminar alumno y todos sus datos','Se borrarán matrículas, asistencia, seguimiento, graduaciones, cuotas, pagos, recibos, pedidos y documentos. También se intentarán borrar sus archivos físicos de Storage.',async()=>{await repos.members.forceDelete(id);toast('Alumno y datos asociados eliminados');await reload();}));
  }catch(e){setError(e);setMainHtml(`${pageHeader(state.session?.rol==='monitor'?'Mis alumnos':'Alumnos')} ${empty('No se pudieron cargar los alumnos',e.message)}`)}
}

export async function renderEnrollments(){
  setMainHtml('<div class="loading-card">Cargando preinscripciones…</div>');
  try{
    const [items,relations]=await Promise.all([repos.preenrollments.list(),loadMemberRelations()]);const can=has(state.session,'enrollmentManage');
    const groups=[{key:'enviada',label:'Nuevas'},{key:'en_revision',label:'En revisión'},{key:'pendiente_documentacion',label:'Documentación'},{key:'lista_espera',label:'Lista de espera'},{key:'aprobada',label:'Aprobadas'}];
    const actions=(p)=>can?`<div class="row-actions">${p.estado==='enviada'?`<button class="btn btn-primary btn-sm approve-pre" data-id="${esc(p.id)}">Aprobar</button><button class="btn btn-ghost btn-sm wait-pre" data-id="${esc(p.id)}">Espera</button><button class="btn btn-ghost btn-sm reject-pre" data-id="${esc(p.id)}">Rechazar</button>`:''}${p.estado!=='aprobada'&&p.estado!=='cancelada'?`<button class="btn btn-ghost btn-sm cancel-pre" data-id="${esc(p.id)}">Cancelar</button>`:''}${['borrador','enviada','rechazada','cancelada'].includes(p.estado)?`<button class="btn btn-danger btn-sm delete-pre" data-id="${esc(p.id)}">Eliminar</button>`:''}</div>`:'';
    const col=(g)=>`<section class="pipeline-col"><h3>${esc(g.label)} · ${items.filter(x=>x.estado===g.key).length}</h3>${items.filter(x=>x.estado===g.key).map(p=>`<div class="pipeline-card"><strong>${esc(p.nombre)} ${esc(p.apellidos)}</strong><small>${esc(relations.disciplines.find(d=>d.id===p.disciplina_id)?.nombre||'Sin disciplina')} · ${dateFmt(p.creado_en)}</small>${p.observaciones?`<p class="muted">${esc(p.observaciones)}</p>`:''}${actions(p)}</div>`).join('')||'<div class="muted" style="font-size:11px;padding:10px 2px">Sin solicitudes</div>'}</section>`;
    const extras=items.filter(x=>!groups.some(g=>g.key===x.estado));
    setMainHtml(`${pageHeader('Preinscripciones','Pipeline de solicitudes y altas',can?'<button class="btn btn-primary" id="new-pre">Nueva preinscripción</button>':'','Gestión')}<div class="pipeline">${groups.map(col).join('')}</div>${extras.length?card('Otros estados',extras.map(x=>quickRow(icon('userPlus'),`${x.nombre} ${x.apellidos}`,x.estado,actions(x))).join('')):''}`);
    const reload=()=>renderEnrollments();
    document.getElementById('new-pre')?.addEventListener('click',()=>openForm({title:'Nueva preinscripción',fields:[{name:'tipo_solicitud',label:'Tipo',type:'select',required:true,value:'adulto',options:[{value:'adulto',label:'Adulto'},{value:'menor',label:'Menor'}]},{name:'nombre',label:'Nombre',required:true},{name:'apellidos',label:'Apellidos',required:true},{name:'fecha_nacimiento',label:'Fecha de nacimiento',type:'date'},{name:'telefono',label:'Teléfono',required:true},{name:'tutor_nombre',label:'Tutor/a'},{name:'tutor_email',label:'Email tutor',type:'email'},{name:'parentesco',label:'Parentesco'},{name:'disciplina_id',label:'Disciplina',type:'select',options:options(relations.disciplines.filter(d=>d.activa))},{name:'grupo_id',label:'Grupo',type:'select',options:options(relations.groups.filter(g=>g.activo))},{name:'tarifa_id',label:'Tarifa',type:'select',options:options(relations.tariffs.filter(t=>t.activa))},{name:'observaciones',label:'Observaciones',type:'textarea',full:true}],onSubmit:async v=>{await repos.preenrollments.create(v);toast('Preinscripción creada');await reload();}}));
    bind('.approve-pre',async(id,el)=>{el.disabled=true;try{await repos.preenrollments.approve(id);toast('Preinscripción aprobada');await reload();}catch(e){setError(e);el.disabled=false;}});
    const reasonAction=(selector,title,fn,required=false)=>bind(selector,id=>openForm({title,fields:[{name:'motivo',label:'Motivo',type:'textarea',full:true,required}],onSubmit:async v=>{await fn(id,v.motivo);toast('Estado actualizado');await reload();}}));
    reasonAction('.wait-pre','Pasar a lista de espera',repos.preenrollments.wait);reasonAction('.reject-pre','Rechazar preinscripción',repos.preenrollments.reject,true);reasonAction('.cancel-pre','Cancelar preinscripción',repos.preenrollments.cancel,true);
    bind('.delete-pre',id=>confirmDialog('Eliminar solicitud','Solo se permite borrar solicitudes no aprobadas. La acción es definitiva.',async()=>{await repos.preenrollments.delete(id);toast('Solicitud eliminada');await reload();},{confirmText:'Eliminar',danger:true}));
  }catch(e){setError(e);setMainHtml(`${pageHeader('Preinscripciones')} ${empty('No se pudieron cargar las solicitudes',e.message)}`)}
}
